PORT=3000
# FRONTEND_URL não é mais necessário — o backend aceita automaticamente
# qualquer porta em localhost/127.0.0.1 (veja src/app.ts).

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=waresync
DB_USER=postgres
DB_PASSWORD=postgres

SESSION_SECRET=troque-este-segredo-em-producao
